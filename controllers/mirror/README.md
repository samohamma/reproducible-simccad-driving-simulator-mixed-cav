# `controllers/mirror/`

Webots mirror controller source and executable.

This controller supports mirror functionality in the participant vehicle model. The compiled executable is retained because it is referenced by the Webots project.
