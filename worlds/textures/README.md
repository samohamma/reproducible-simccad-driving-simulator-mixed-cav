# `worlds/textures/`

This folder contains texture assets referenced by the Webots worlds and PROTO files.

The benchmark workflow does not modify these files. Missing textures can affect visual appearance and rendering load, so texture files should be preserved when sharing or moving the project.
