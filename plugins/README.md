# `plugins/`

Webots plugin/helper folder.

The project includes an automobile robot-window plugin under `robot_windows/`. The compiled plugin artefact is retained for compatibility with the original Webots project.
