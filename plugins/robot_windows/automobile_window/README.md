# `plugins/robot_windows/automobile_window/`

Source and compiled DLL for the automobile robot-window plugin.

The root-level `automobile_window.dll` is retained. Intermediate build artefacts were removed from the cleaned release package because they are not required for running the current benchmark workflow.
